# v0.0.3

A small maintenance release that fixes the Chrome Web Store publishing configuration so the extension can be published and updated correctly.

## Fixes

- Fixed the **Chrome Web Store** extension key configuration to ensure correct publishing and updates
